{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["local>contentful/renovate-config"],
  "packageRules": [
    {
      "$comment": "These devDependencies are managed and regenerated by Fern. Manual Renovate updates would be overwritten on the next Fern run, so we disable them here and let Fern own the versions.",
      "matchPackageNames": [
        "webpack",
        "ts-loader",
        "vitest",
        "@types/node",
        "typescript",
        "@biomejs/biome",
        "license-checker-rseidelsohn"
      ],
      "enabled": false
    }
  ]
}
